resources:
  - name: aws-concourse
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/aws-concourse.git
    check_every: 15m
    webhook_token: ((dataworks.concourse_github_webhook_token))
